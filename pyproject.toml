[project]
name = "seibro-foreign-flows"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "polars>=1.40.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
